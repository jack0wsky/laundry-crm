on: [pull_request]

jobs:
  test:
    runs-on: 'ubuntu-latest'
    name: 'Run tests'
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2

      - name: 'Install dependencies'
        run: npm install

      - name: 'Run tests'
        run: npm test